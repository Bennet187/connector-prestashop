name: CI

on:
  push:
    branches:
    - feature/github-actions
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: PHP- und Composer-Setup
        uses: shivammathur/setup-php@v1
        with: 
          php-version: '7.2'
          ini-values: phar.readonly=Off
      - run: |
            sudo apt-get update
            sudo apt-get install apache2
            sudo add-apt-repository -y ppa:ondrej/php
            sudo apt-get install php7.2-intl
      - uses: docker://composer:latest
        
      - name: Repo auschecken
        uses: actions/checkout@v2
        with:
          repository: 'Bennet187/connector-prestashop'
          ref: 'develop'
          clean: 'false'
          
      - name: Prestashop-Installation Test
        run: |
            cd /var/www/html
            sudo wget -nv https://codeload.github.com/PrestaShop/PrestaShop/zip/1.7.6.3
            sudo unzip -qq 1.7.6.3
            mysql -u root -proot -e 'CREATE DATABASE prestashop'
            sudo mv /var/www/html/PrestaShop-1.7.6.3/* /var/www/html/
            cd /var/www/html/PrestaShop-1.7.6.3
            cd /var/www/html
            sudo chmod -R 777 /var/www/
            composer install
            cd /var/www/html/install-dev
            sudo php index_cli.php --domain=localhost --db_server=localhost --db_name=prestashop --db_user=root --db_password=root

      - name: Buildprozess
        run: |
            cd $GITHUB_WORKSPACE
            sudo composer update --no-dev
            sudo php ./phing.phar release
            sudo composer update
            mkdir zip
            mv *.zip $GITHUB_WORKSPACE/zip
            
      - name: Connector installieren
        run: |
            cd /var/www/html
            sudo mkdir --parents /var/www/html/modules/jtlconnector
            sudo mv $GITHUB_WORKSPACE/* $_     
            sudo mv /var/www/html/modules/jtlconnector/zip $GITHUB_WORKSPACE
            cd /var/www/html/modules/jtlconnector
            sudo mkdir logs
            sudo chmod -R 777 logs
            composer update
            cd /var/www/html
            sudo php bin/console prestashop:module install jtlconnector
      - name: Virtuellen Host eintragen
        run:  printf "
            <VirtualHost *:80>
                ServerName prestatest.lan
                DocumentRoot /var/www/html
            </VirtualHost> 
            <Directory /var/www/html/>
                AllowOverride All
            </Directory>" | sudo tee /etc/apache2/sites-available/prestatest.lan.conf
      - run: |
          echo "127.0.0.1  prestatest.lan" >> /etc/hosts
          sudo a2ensite prestatest.lan.conf
          sudo systemctl reload apache2
          cd /var/www/html/vendor/bin/
          printf "{"connector_url": "prestatest.lan", "connector_token": ""} | sudo tee /var/www/html/vendor/bin/phpunit-conf.json
          ./phpunit

      - if: success()
        name: FTP Upload
        uses: SamKirkland/FTP-Deploy-Action@2.0.0
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USER }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          ARGS: --verbose 
          REMOTE_DIR: build
          LOCAL_DIR: zip
